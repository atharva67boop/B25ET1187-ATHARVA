#include <iostream>
using namespace std;

class Complex
{
    float real, imag;

public:
    void input()
    {
        cin >> real >> imag;
    }

    Complex add(Complex c1, Complex c2)
    {
        Complex temp;
        temp.real = real + c1.real + c2.real;
        temp.imag = imag + c1.imag + c2.imag;
        return temp;
    }

    void display()
    {
        cout << real << " + " << imag << "i";
    }
};

int main()
{
    Complex c1, c2, c3, sum;

    cout << "Enter first complex number: ";
    c1.input();

    cout << "Enter second complex number: ";
    c2.input();

    cout << "Enter third complex number: ";
    c3.input();

    sum = c1.add(c2, c3);

    cout << "Sum = ";
    sum.display();

    return 0;
}
